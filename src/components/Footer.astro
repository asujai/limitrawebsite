---
import { translations, SUPPORTED_LANGUAGES, type SupportedLang } from '../data/translations';

interface Props {
  currentLang?: SupportedLang;
}

const pathname = Astro.url.pathname.replace(/\/$/, '') || '/';
const pathSegments = pathname.split('/').filter(Boolean);
const firstSegment = pathSegments[0] as SupportedLang;

const currentLang: SupportedLang = Astro.props.currentLang || (
  SUPPORTED_LANGUAGES.some(l => l.code === firstSegment) ? firstSegment : 'tr'
);

const t = translations[currentLang] || translations.en;
const prefix = currentLang === 'tr' ? '' : `/${currentLang}`;
const currentYear = new Date().getFullYear();

const newsHref = currentLang === 'tr' ? '/haberler' : `${prefix}/news`;
const homeHref = currentLang === 'tr' ? '/' : `${prefix}`;

const groups = [
  {
    title: t.footer.productTitle,
    links: [
      { href: `${prefix}/limitra`, label: t.nav.howItWorks },
      { href: `${prefix}/sss`, label: t.nav.faq },
      { href: currentLang === 'tr' ? '/iletisim' : `${prefix}/iletisim`, label: currentLang === 'tr' ? 'İletişim' : 'Contact' }
    ]
  },
  {
    title: t.footer.exploreTitle,
    links: [
      { href: newsHref, label: t.nav.news },
      { href: currentLang === 'tr' ? '/bilgi-merkezi' : `${prefix}/bilgi-merkezi`, label: t.nav.guides },
      { href: currentLang === 'tr' ? '/bilgi-merkezi/ekran-suresi' : `${prefix}/bilgi-merkezi/ekran-suresi`, label: currentLang === 'tr' ? 'Ekran süresi rehberi' : 'Screen time guide' }
    ]
  },
  {
    title: t.footer.legalTitle,
    links: [
      { href: currentLang === 'tr' ? '/gizlilik-politikasi' : `${prefix}/gizlilik-politikasi`, label: currentLang === 'tr' ? 'Gizlilik politikası' : 'Privacy policy' },
      { href: currentLang === 'tr' ? '/kullanim-sartlari' : `${prefix}/kullanim-sartlari`, label: currentLang === 'tr' ? 'Kullanım şartları' : 'Terms of use' }
    ]
  },
  {
    title: 'Limitra Social',
    links: [
      { href: '/limitra-social/privacy', label: currentLang === 'tr' ? 'Gizlilik politikası' : 'Privacy policy' },
      { href: '/limitra-social/terms', label: currentLang === 'tr' ? 'Kullanım şartları' : 'Terms of use' },
      { href: '/limitra-social/delete-account', label: currentLang === 'tr' ? 'Hesap ve veri silme' : 'Delete account & data' }
    ]
  }
];
---

<footer class="footer">
  <div class="container footer-top">
    <div class="footer-brand">
      <a href={homeHref} class="brand">
        <img src="/logo.jpg" alt="Limitra App Block Logo" class="brand-logo" width="36" height="36" />
        <span class="brand-text">Limitra <span class="brand-badge">App Block</span></span>
      </a>
      <p>{t.hero.lead}</p>
      <a class="play-link" href="https://play.google.com/store/apps/details?id=com.gardiyan.app" target="_blank" rel="noopener noreferrer">
        {t.hero.ctaPrimary} <span aria-hidden="true">↗</span>
      </a>
    </div>

    <div class="footer-links">
      {groups.map((group) => (
        <div class="link-group">
          <h2>{group.title}</h2>
          <ul>
            {group.links.map((link) => <li><a href={link.href}>{link.label}</a></li>)}
          </ul>
        </div>
      ))}
    </div>
  </div>

  <div class="container footer-bottom">
    <p>&copy; {currentYear} Limitra App Block. {t.footer.rights}</p>
    <p class="tagline">{t.footer.tagline}</p>
  </div>
</footer>

<style>
  .footer {
    background: #09162f;
    color: #e2e8f0;
    padding-top: 5rem;
    padding-bottom: 3rem;
    margin-top: 6rem;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .footer-top {
    display: grid;
    grid-template-columns: 1.4fr 2fr;
    gap: 4rem;
    padding-bottom: 4rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .footer-brand {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.25rem;
  }

  .footer-brand .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    text-decoration: none;
    font-weight: 700;
    color: #ffffff;
    font-size: 1.25rem;
  }

  .footer-brand .brand-logo {
    border-radius: 8px;
  }

  .footer-brand .brand-badge {
    font-size: 0.72rem;
    font-weight: 600;
    background: rgba(99, 102, 241, 0.2);
    color: #a5b4fc;
    padding: 0.2rem 0.5rem;
    border-radius: 9999px;
    border: 1px solid rgba(99, 102, 241, 0.3);
  }

  .footer-brand p {
    color: #94a3b8;
    line-height: 1.6;
    margin: 0;
    font-size: 0.95rem;
    max-width: 360px;
  }

  .play-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    background: rgba(255, 255, 255, 0.1);
    color: #ffffff;
    padding: 0.6rem 1.25rem;
    border-radius: 9999px;
    font-size: 0.88rem;
    font-weight: 600;
    text-decoration: none;
    border: 1px solid rgba(255, 255, 255, 0.15);
    transition: all 0.2s ease;
  }

  .play-link:hover {
    background: rgba(255, 255, 255, 0.18);
    border-color: rgba(255, 255, 255, 0.3);
  }

  .footer-links {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
  }

  .link-group h2 {
    color: #ffffff;
    font-size: 0.95rem;
    font-weight: 600;
    margin: 0 0 1.25rem 0;
    letter-spacing: 0.02em;
    font-family: inherit;
  }

  .link-group ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .link-group a {
    color: #94a3b8;
    text-decoration: none;
    font-size: 0.88rem;
    transition: color 0.2s ease;
  }

  .link-group a:hover {
    color: #ffffff;
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 2.5rem;
    color: #64748b;
    font-size: 0.85rem;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .footer-bottom p {
    margin: 0;
  }

  .tagline {
    color: #94a3b8;
  }

  @media (max-width: 900px) {
    .footer-top {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .footer-links {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }
  }

  @media (max-width: 540px) {
    .footer-links {
      grid-template-columns: 1fr;
    }
  }
</style>
